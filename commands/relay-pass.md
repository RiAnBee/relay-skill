---
description: Write and validate a Relay handoff artifact using wire schema v2 for a zero-context receiver.
argument-hint: "[focus, hint, or next task] [--keep|--persist|--tmp|--temp] [--full|--compact|--brief]"
---

Use the installed `relay-pass` skill to handle this request.

Relay pass arguments: $ARGUMENTS

If the skill is not automatically loaded, locate the installed `relay-pass`
skill directory and read its `SKILL.md`. Do not assume the user's current
project contains this repository's `skills/` path.
