---
description: Pick up from a Relay handoff document and continue work.
argument-hint: "[file path, focus, hint, or next task]"
---

Use the installed `relay-pickup` skill to handle this request.

Relay pickup arguments: $ARGUMENTS

If the skill is not automatically loaded, read `skills/relay-pickup/SKILL.md` and follow its instructions.
