---
description: Select, validate, reconcile, and continue from a Relay handoff artifact.
argument-hint: "[file path, focus, hint, or next task]"
---

Use the installed `relay-pickup` skill to handle this request.

Relay pickup arguments: $ARGUMENTS

If the skill is not automatically loaded, locate the installed `relay-pickup`
skill directory and read its `SKILL.md`. Do not assume the user's current
project contains this repository's `skills/` path.
