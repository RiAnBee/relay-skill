---
description: Pass or pick up a portable, verifiable Relay handoff artifact.
argument-hint: "pass|pickup [focus, hint, or next task] [--keep|--persist|--tmp|--temp] [--full|--compact|--brief]"
---

Use the installed `relay` skill to handle this request.

Relay arguments: $ARGUMENTS

If the skill is not automatically loaded, locate the installed `relay` skill
directory and read its `SKILL.md`. Do not assume the user's current project
contains this repository's `skills/` path.
