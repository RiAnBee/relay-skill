---
description: Configure Relay project defaults.
argument-hint: "[project|keep|persist|tmp|temp] [compact|brief|full] [show|reset]"
---

Use the installed `relay-set` skill to handle this request.

Relay set arguments: $ARGUMENTS

If the skill is not automatically loaded, read `skills/relay-set/SKILL.md` and follow its instructions.
