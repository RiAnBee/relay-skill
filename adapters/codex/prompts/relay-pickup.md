---
description: Pick up from a Relay handoff document in Codex.
argument-hint: "[file path, focus, hint, or next task]"
---

Use the `relay-pickup` skill to handle this request.

Relay pickup arguments: $ARGUMENTS

If the skill is not available, follow the pickup-mode behavior described in `skills/relay-pickup/SKILL.md`.
