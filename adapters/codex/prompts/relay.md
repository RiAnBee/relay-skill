---
description: Pass or pick up a Relay handoff document.
argument-hint: "pass|pickup [focus, hint, or next task] [--keep|--persist] [--full]"
---

Use the canonical Relay instructions from `skills/relay/SKILL.md` to handle this request.

Relay arguments: $ARGUMENTS

If the canonical skill file is not available in this workspace, follow the Relay behavior described in the installed `relay` skill.
