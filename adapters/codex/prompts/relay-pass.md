---
description: Write a Relay handoff document for the next Codex session.
argument-hint: "[focus, hint, or next task] [--keep|--persist] [--full]"
---

Use the `relay-pass` skill to handle this request.

Relay pass arguments: $ARGUMENTS

If the skill is not available, follow the pass-mode behavior described in `skills/relay-pass/SKILL.md`.
